<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="youngun.tis.travel.management.dao.mapper.UserMapper">
	<resultMap id="userMap" type="user">
		<id property="memberNum" column="member_num" />
		<result property="userName" column="user_name"/>
		<result property="userId" column="user_id"/>
		<result property="password" column="pw"/>
	</resultMap>
	
	<insert id="insertUser" parameterType="User" keyProperty="userNum">
		insert into members(userName, userId, password, birth, phoneNum, email,
			nickName, gender)
		values(name, id, password, birth, phone, email, nickname, gender);
	</insert>
	
</mapper>